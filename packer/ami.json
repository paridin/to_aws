{
    "variables": {
        "env": "dev"
    },
    "builders": [
        {
            "type": "amazon-ebs",
            "region": "us-east-1",
            "source_ami": "ami-0ac019f4fcb7cb7e6",
            "instance_type": "t2.micro",
            "ssh_username": "ubuntu",
            "ami_name": "elixir-phx-{{timestamp | clean_ami_name}}",
            "ami_description": "AMI designed to run Elixir and Phoenix Apps - {{user `env`}}",
            "tags": {
                "env": "{{user `env`}}",
                "role": "FreeTier"
            },
            "run_tags": {
                "role": "buildSystem"
            }
        }
    ]
}